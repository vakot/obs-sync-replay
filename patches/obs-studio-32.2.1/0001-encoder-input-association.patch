diff --git a/libobs/media-io/video-io.c b/libobs/media-io/video-io.c
index ec6b424..bcf1487 100644
--- a/libobs/media-io/video-io.c
+++ b/libobs/media-io/video-io.c
@@ -155,8 +155,10 @@ static inline bool video_output_cur_frame(struct video_output *video)
 		if (skip)
 			continue;
 
-		if (scale_video_output(input, &frame))
+		if (scale_video_output(input, &frame)) {
+			frame.source_frame_id = frame_info->frame.source_frame_id;
 			input->callback(input->param, &frame);
+		}
 	}
 
 	pthread_mutex_unlock(&video->input_mutex);
@@ -166,6 +168,7 @@ static inline bool video_output_cur_frame(struct video_output *video)
 	pthread_mutex_lock(&video->data_mutex);
 
 	frame_info->frame.timestamp += video->frame_time;
+	frame_info->frame.source_frame_id += 1;
 	complete = --frame_info->count == 0;
 	skipped = frame_info->skipped > 0;
 
@@ -506,7 +509,8 @@ const struct video_output_info *video_output_get_info(const video_t *video)
 	return video ? &video->info : NULL;
 }
 
-bool video_output_lock_frame(video_t *video, struct video_frame *frame, int count, uint64_t timestamp)
+bool video_output_lock_frame2(video_t *video, struct video_frame *frame, int count, uint64_t timestamp,
+				      uint64_t source_frame_id)
 {
 	struct cached_frame_info *cfi;
 	bool locked;
@@ -531,6 +535,7 @@ bool video_output_lock_frame(video_t *video, struct video_frame *frame, int coun
 
 		cfi = &video->cache[video->last_added];
 		cfi->frame.timestamp = timestamp;
+		cfi->frame.source_frame_id = source_frame_id;
 		cfi->count = count;
 		cfi->skipped = 0;
 
@@ -544,6 +549,11 @@ bool video_output_lock_frame(video_t *video, struct video_frame *frame, int coun
 	return locked;
 }
 
+bool video_output_lock_frame(video_t *video, struct video_frame *frame, int count, uint64_t timestamp)
+{
+	return video_output_lock_frame2(video, frame, count, timestamp, 0);
+}
+
 void video_output_unlock_frame(video_t *video)
 {
 	if (!video)
diff --git a/libobs/media-io/video-io.h b/libobs/media-io/video-io.h
index fde6ca7..89e8fb9 100644
--- a/libobs/media-io/video-io.h
+++ b/libobs/media-io/video-io.h
@@ -122,6 +122,7 @@ struct video_data {
 	uint8_t *data[MAX_AV_PLANES];
 	uint32_t linesize[MAX_AV_PLANES];
 	uint64_t timestamp;
+	uint64_t source_frame_id;
 };
 
 struct video_output_info {
@@ -312,6 +313,8 @@ EXPORT bool video_output_active(const video_t *video);
 
 EXPORT const struct video_output_info *video_output_get_info(const video_t *video);
 EXPORT bool video_output_lock_frame(video_t *video, struct video_frame *frame, int count, uint64_t timestamp);
+EXPORT bool video_output_lock_frame2(video_t *video, struct video_frame *frame, int count,
+					     uint64_t timestamp, uint64_t source_frame_id);
 EXPORT void video_output_unlock_frame(video_t *video);
 EXPORT uint64_t video_output_get_frame_time(const video_t *video);
 EXPORT void video_output_stop(video_t *video);
diff --git a/libobs/obs-encoder.c b/libobs/obs-encoder.c
index 403f01e..1967f12 100644
--- a/libobs/obs-encoder.c
+++ b/libobs/obs-encoder.c
@@ -829,6 +829,9 @@ static inline void obs_encoder_start_internal(obs_encoder_t *encoder, encoded_ca
 		pause_reset(&encoder->pause);
 
 		encoder->cur_pts = 0;
+		pthread_mutex_lock(&encoder->callbacks_mutex);
+		encoder->next_input_request_id = 0;
+		pthread_mutex_unlock(&encoder->callbacks_mutex);
 		add_connection(encoder);
 	}
 }
@@ -1210,6 +1213,27 @@ void obs_encoder_set_video(obs_encoder_t *encoder, video_t *video)
 	encoder_set_video(encoder, video);
 }
 
+bool obs_encoder_set_input_callback(obs_encoder_t *encoder, obs_encoder_input_callback_t callback,
+					    void *param)
+{
+	if (!obs_encoder_valid(encoder, "obs_encoder_set_input_callback"))
+		return false;
+	if (encoder->info.type != OBS_ENCODER_VIDEO)
+		return false;
+	if (encoder_active(encoder)) {
+		blog(LOG_WARNING, "encoder '%s': Cannot change input callback while active",
+		     obs_encoder_get_name(encoder));
+		return false;
+	}
+
+	pthread_mutex_lock(&encoder->callbacks_mutex);
+	encoder->input_callback = callback;
+	encoder->input_callback_param = param;
+	encoder->next_input_request_id = 0;
+	pthread_mutex_unlock(&encoder->callbacks_mutex);
+	return true;
+}
+
 static void encoder_set_video(obs_encoder_t *encoder, video_t *video)
 {
 	const struct video_output_info *voi;
@@ -1453,7 +1477,43 @@ void send_off_encoder_packet(obs_encoder_t *encoder, bool success, bool received
 }
 
 static const char *do_encode_name = "do_encode";
-bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const uint64_t *frame_cts)
+void obs_encoder_input_begin(obs_encoder_t *encoder, struct obs_encoder_input *input,
+				     uint64_t source_frame_id, uint64_t composition_cts)
+{
+	obs_encoder_input_callback_t callback;
+	void *param;
+
+	memset(input, 0, sizeof(*input));
+	input->source_frame_id = source_frame_id;
+	input->composition_cts = composition_cts;
+	input->encoder_pts = encoder->cur_pts;
+
+	pthread_mutex_lock(&encoder->callbacks_mutex);
+	input->request_id = ++encoder->next_input_request_id;
+	callback = encoder->input_callback;
+	param = encoder->input_callback_param;
+	pthread_mutex_unlock(&encoder->callbacks_mutex);
+
+	if (callback)
+		callback(encoder, input, param);
+}
+
+void obs_encoder_input_record(struct obs_encoder *encoder, const struct obs_encoder_input *input,
+				      uint64_t fer, uint64_t ferc)
+{
+	struct encoder_packet_time *ept = da_push_back_new(encoder->encoder_packet_times);
+	ept->pts = input->encoder_pts;
+	ept->cts = input->composition_cts;
+	ept->fer = fer;
+	ept->ferc = ferc;
+	ept->source_frame_id = input->source_frame_id;
+	ept->request_id = input->request_id;
+	ept->association_id = input->association_id;
+	ept->has_association = input->has_association;
+}
+
+bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const uint64_t *frame_cts,
+		       uint64_t source_frame_id)
 {
 	profile_start(do_encode_name);
 	if (!encoder->profile_encoder_encode_name)
@@ -1464,6 +1524,7 @@ bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const u
 	bool received = false;
 	bool success;
 	uint64_t fer_ts = 0;
+	struct obs_encoder_input input;
 
 	if (encoder->reconfigure_requested) {
 		encoder->reconfigure_requested = false;
@@ -1478,6 +1539,8 @@ bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const u
 	 * needs to be read just before the encode request.
 	 */
 	fer_ts = os_gettime_ns();
+	if (frame_cts)
+		obs_encoder_input_begin(encoder, &input, source_frame_id, *frame_cts);
 
 	profile_start(encoder->profile_encoder_encode_name);
 	success = encoder->info.encode(encoder->context.data, frame, &pkt, &received);
@@ -1488,17 +1551,9 @@ bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const u
 	 * (frame encode request complete) and current PTS. PTS is used to
 	 * associate the frame timing data with the encode packet. */
 	if (frame_cts) {
-		struct encoder_packet_time *ept = da_push_back_new(encoder->encoder_packet_times);
 		// Get the frame encode request complete timestamp
-		if (success) {
-			ept->ferc = os_gettime_ns();
-		} else {
-			// Encode had error, set ferc to 0
-			ept->ferc = 0;
-		}
-		ept->pts = frame->pts;
-		ept->cts = *frame_cts;
-		ept->fer = fer_ts;
+		const uint64_t ferc = success ? os_gettime_ns() : 0;
+		obs_encoder_input_record(encoder, &input, fer_ts, ferc);
 	}
 	send_off_encoder_packet(encoder, success, received, &pkt);
 
@@ -1633,7 +1688,7 @@ static void receive_video(void *param, struct video_data *frame)
 	enc_frame.frames = 1;
 	enc_frame.pts = encoder->cur_pts;
 
-	if (do_encode(encoder, &enc_frame, &frame->timestamp))
+	if (do_encode(encoder, &enc_frame, &frame->timestamp, frame->source_frame_id))
 		encoder->cur_pts += encoder->timebase_num * encoder->frame_rate_divisor;
 
 wait_for_audio:
@@ -1761,7 +1816,7 @@ static bool send_audio_data(struct obs_encoder *encoder)
 	enc_frame.frames = (uint32_t)encoder->framesize;
 	enc_frame.pts = encoder->cur_pts;
 
-	if (!do_encode(encoder, &enc_frame, NULL))
+	if (!do_encode(encoder, &enc_frame, NULL, 0))
 		return false;
 
 	encoder->cur_pts += encoder->framesize;
diff --git a/libobs/obs-encoder.h b/libobs/obs-encoder.h
index 73b139c..d2dbf7a 100644
--- a/libobs/obs-encoder.h
+++ b/libobs/obs-encoder.h
@@ -94,8 +94,39 @@ struct encoder_packet_time {
 	 * and packet interleaving.
 	 */
 	uint64_t pir;
+
+	/* Source identity supplied by the video submission path. */
+	uint64_t source_frame_id;
+
+	/* Stable identity for this encoder input request. */
+	uint64_t request_id;
+
+	/* Opaque caller association set by obs_encoder_input_callback_t. */
+	uint64_t association_id;
+	bool has_association;
+};
+
+struct obs_encoder_input {
+	/* Monotonic source-slot identity assigned by libobs before queueing. */
+	uint64_t source_frame_id;
+
+	/* Composition timestamp associated with this input. */
+	uint64_t composition_cts;
+
+	/* Encoder-local PTS for this request. */
+	int64_t encoder_pts;
+
+	/* Monotonic identity unique to this encoder input request. */
+	uint64_t request_id;
+
+	/* Caller-owned opaque association copied into encoder_packet_time. */
+	uint64_t association_id;
+	bool has_association;
 };
 
+typedef void (*obs_encoder_input_callback_t)(obs_encoder_t *encoder,
+							 struct obs_encoder_input *input, void *param);
+
 /** Encoder output packet */
 struct encoder_packet {
 	uint8_t *data; /**< Packet data */
diff --git a/libobs/obs-internal.h b/libobs/obs-internal.h
index ca5bdac..e3ed4b3 100644
--- a/libobs/obs-internal.h
+++ b/libobs/obs-internal.h
@@ -311,6 +311,7 @@ extern void obs_display_free(struct obs_display *display);
 
 struct obs_vframe_info {
 	uint64_t timestamp;
+	uint64_t source_frame_id;
 	int count;
 };
 
@@ -319,6 +320,7 @@ struct obs_tex_frame {
 	gs_texture_t *tex_uv;
 	uint32_t handle;
 	uint64_t timestamp;
+	uint64_t source_frame_id;
 	uint64_t lock_key;
 	int count;
 	bool released;
@@ -409,6 +411,7 @@ struct obs_core_video {
 	pthread_t video_thread;
 	uint32_t total_frames;
 	uint32_t lagged_frames;
+	uint64_t next_source_frame_id;
 	bool thread_initialized;
 
 	gs_texture_t *transparent_texture;
@@ -1409,6 +1412,10 @@ struct obs_encoder {
 	// Number of frames successfully encoded
 	uint32_t encoded_frames;
 
+	obs_encoder_input_callback_t input_callback;
+	void *input_callback_param;
+	uint64_t next_input_request_id;
+
 	/* Regions of interest to prioritize during encoding */
 	pthread_mutex_t roi_mutex;
 	DARRAY(struct obs_encoder_roi) roi;
@@ -1469,9 +1476,15 @@ extern void obs_encoder_remove_output(struct obs_encoder *encoder, struct obs_ou
 extern bool start_gpu_encode(obs_encoder_t *encoder);
 extern void stop_gpu_encode(obs_encoder_t *encoder);
 
-extern bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const uint64_t *frame_cts);
+extern bool do_encode(struct obs_encoder *encoder, struct encoder_frame *frame, const uint64_t *frame_cts,
+			      uint64_t source_frame_id);
 extern void send_off_encoder_packet(obs_encoder_t *encoder, bool success, bool received, struct encoder_packet *pkt);
 
+extern void obs_encoder_input_begin(obs_encoder_t *encoder, struct obs_encoder_input *input,
+					    uint64_t source_frame_id, uint64_t composition_cts);
+extern void obs_encoder_input_record(struct obs_encoder *encoder,
+					     const struct obs_encoder_input *input, uint64_t fer, uint64_t ferc);
+
 void obs_encoder_destroy(obs_encoder_t *encoder);
 
 /* ------------------------------------------------------------------------- */
diff --git a/libobs/obs-video-gpu-encode.c b/libobs/obs-video-gpu-encode.c
index 283c815..39a2079 100644
--- a/libobs/obs-video-gpu-encode.c
+++ b/libobs/obs-video-gpu-encode.c
@@ -81,6 +81,7 @@ static void *gpu_encode_thread(void *data)
 			bool received = false;
 			bool success = false;
 			uint32_t skip = 0;
+			struct obs_encoder_input input;
 
 			obs_encoder_t *encoder = encoders.array[i];
 			obs_weak_encoder_t **paired = encoder->paired_encoders.array;
@@ -150,6 +151,7 @@ static void *gpu_encode_thread(void *data)
 			 * needs to be read just before the encode request.
 			 */
 			fer_ts = os_gettime_ns();
+			obs_encoder_input_begin(encoder, &input, tf.source_frame_id, tf.timestamp);
 
 			profile_start(gpu_encode_frame_name);
 			if (encoder->info.encode_texture2) {
@@ -172,20 +174,7 @@ static void *gpu_encode_thread(void *data)
 			 * the CTS (composition time), FER (frame encode request), FERC
 			 * (frame encode request complete) and current PTS. PTS is used to
 			 * associate the frame timing data with the encode packet. */
-			if (tf.timestamp) {
-				struct encoder_packet_time *ept = da_push_back_new(encoder->encoder_packet_times);
-				// Get the frame encode request complete timestamp
-				if (success) {
-					ept->ferc = os_gettime_ns();
-				} else {
-					// Encode had error, set ferc to 0
-					ept->ferc = 0;
-				}
-
-				ept->pts = encoder->cur_pts;
-				ept->cts = tf.timestamp;
-				ept->fer = fer_ts;
-			}
+			obs_encoder_input_record(encoder, &input, fer_ts, success ? os_gettime_ns() : 0);
 
 			send_off_encoder_packet(encoder, success, received, &pkt);
 
@@ -202,6 +191,7 @@ static void *gpu_encode_thread(void *data)
 
 		if (--tf.count) {
 			tf.timestamp += interval;
+			tf.source_frame_id += 1;
 			deque_push_front(&video->gpu_encoder_queue, &tf, sizeof(tf));
 
 			video_output_inc_texture_skipped_frames(video->video);
diff --git a/libobs/obs-video.c b/libobs/obs-video.c
index eca6f83..36e0885 100644
--- a/libobs/obs-video.c
+++ b/libobs/obs-video.c
@@ -494,6 +494,7 @@ static inline bool queue_frame(struct obs_core_video_mix *video, bool raw_active
 
 	tf.count = 1;
 	tf.timestamp = vframe_info->timestamp;
+	tf.source_frame_id = vframe_info->source_frame_id;
 	tf.released = true;
 #ifdef _WIN32
 	tf.handle = gs_texture_get_shared_handle(tf.tex);
@@ -504,6 +505,7 @@ static inline bool queue_frame(struct obs_core_video_mix *video, bool raw_active
 	os_sem_post(video->gpu_encode_semaphore);
 
 finish:
+	vframe_info->source_frame_id += 1;
 	return --vframe_info->count;
 }
 
@@ -782,7 +784,8 @@ static inline void output_video_data(struct obs_core_video_mix *video, struct vi
 
 	info = video_output_get_info(video->video);
 
-	locked = video_output_lock_frame(video->video, &output_frame, count, input_frame->timestamp);
+	locked = video_output_lock_frame2(video->video, &output_frame, count, input_frame->timestamp,
+						input_frame->source_frame_id);
 	if (locked) {
 		if (video->gpu_conversion) {
 			set_gpu_converted_data(&output_frame, input_frame, info);
@@ -825,7 +828,9 @@ static inline void video_sleep(struct obs_core_video *video, uint64_t *p_time, u
 	video->lagged_frames += count - 1;
 
 	vframe_info.timestamp = cur_time;
+	vframe_info.source_frame_id = video->next_source_frame_id;
 	vframe_info.count = count;
+	video->next_source_frame_id += (uint64_t)count;
 
 	pthread_mutex_lock(&video->encoder_group_mutex);
 	for (size_t i = 0; i < video->ready_encoder_groups.num; i++) {
@@ -909,6 +912,7 @@ static inline void output_frame(struct obs_core_video_mix *video)
 		deque_pop_front(&video->vframe_info_buffer, &vframe_info, sizeof(vframe_info));
 
 		frame.timestamp = vframe_info.timestamp;
+		frame.source_frame_id = vframe_info.source_frame_id;
 		profile_start(output_frame_output_video_data_name);
 		output_video_data(video, &frame, vframe_info.count);
 		profile_end(output_frame_output_video_data_name);
	}
 
 	if (++video->cur_texture == NUM_TEXTURES)
 		video->cur_texture = 0;
}

diff --git a/libobs/obs.h b/libobs/obs.h
index 4e51eb4..9c68a36 100644
--- a/libobs/obs.h
+++ b/libobs/obs.h
@@ -2412,6 +2412,17 @@ EXPORT obs_data_t *obs_encoder_get_settings(const obs_encoder_t *encoder);
 /** Sets the video output context to be used with this encoder */
 EXPORT void obs_encoder_set_video(obs_encoder_t *encoder, video_t *video);
 
+/**
+ * Sets the callback used to associate each submitted video input with an
+ * opaque caller identity. The callback runs synchronously immediately before
+ * the registered encoder implementation is invoked on its raw or texture
+ * submission thread. It must not block or retain the input pointer. The
+ * callback and parameter remain installed until replaced with NULL or the
+ * encoder is released, and must remain valid while the encoder is active.
+ */
+EXPORT bool obs_encoder_set_input_callback(obs_encoder_t *encoder,
+						   obs_encoder_input_callback_t callback, void *param);
+
 /** Sets the audio output context to be used with this encoder */
 EXPORT void obs_encoder_set_audio(obs_encoder_t *encoder, audio_t *audio);
 
